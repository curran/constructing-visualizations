import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  // Base config needed for GitHub Pages deploy
  base: '/constructing-visualizations/',
})
