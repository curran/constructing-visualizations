We have our examples, including the clickable circles, where when you click, it changes the style of the circle. Now this is a predecessor to interactive visualizations where you could click on things like, I don't know, elements in a color legend. But let's also create another example called hovering, where we start by making a copy of the clickable circles example, increment the number to 10, follow the existing naming convention, and make it so that when you hover over these circles, they change style.
