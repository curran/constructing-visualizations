In the examples directory, we have a sequence of numbered examples. Please create the next one in the sequence, following the established naming convention and index, and make it as follows:

What we want to do next is make a new example based on a combination of the pseudo scatter plot, which has these circles distributed across a fixed width and height of nine, I think it's 960 by 500, and this other example responding to resize, which has the resize observer that is working well with, you know, just drawing this X. But this new example should not have the X, so no lines, and it should have the circles, the same circles as the pseudo scatter plot. But the key difference is D3 will be responsible for doing the rendering of these circles, and it will be data-driven. So we should have a constant in the JavaScript like data equals an array of objects with x and y coordinates. And then in the rendering, we should use D3 scales, D3 linear scales, to stretch these points over the screen. So what we should end up with is a useEffect hook that uses a useRef from D3, sorry, a useRef from React, to integrate D3. So we should make a D3 selection of the ref, which is the SVG element, and then use D3 selection to render the circles, but first process the data through these D3 scales. So that the net effect should be that as you increase or decrease the browser zoom or resize the window, it should automatically stretch these circles across the screen.