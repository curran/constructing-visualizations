Change the implementation of the loading and summarizing data component so that instead of having a use state where we track the summary of the data, I want to actually have a use state where we track the data itself, the thing that gets returned from D3 CSV parse, so that this is something that we could build on in the future. And then the summary should be a derived value from the data. Maybe we could use useMemo for that to memoize that value.
