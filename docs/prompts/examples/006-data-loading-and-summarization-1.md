Okay, so we have these examples in the source directory under examples, and what we want to do next is create the next example in the series. So it would be 006, and let's call this Loading and Summarizing Data. And what we want this example to do is first we should start by making a copy of the previous example, Responsive Pseudo Scatter Plot, and then modify it so that we just use the dimensions to figure out the center point. And at that center point, we put a text element that is centered on that point, and in this SVG text element, it should just say Rows: followed by the count of the rows in the dataset, and then Columns: followed by the count of that. So the dataset that we want to load is under public datasets, Palmer Penguins, Penguins.csv. This is the dataset that we want to load and summarize. Once you're done with the implementation, please use Playwright in headed mode to access the localhost server, the dev server, and validate that the program is working correctly.
