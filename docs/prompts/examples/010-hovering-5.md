In the scatterplot example we started getting at some of the ideas around organizing the group elements and giving themathes we have marks the axes the labels What I want to do is copy this structure Were the hovering example So that within SV we have defined with JS Layers which are group elements and even I want to take it one step further and refactor into different compon You knowing top level component we should see the SV and then within that I want to see two compon One of them called circles and the other one called Voronoi overly Each return group elements with a class name that matches the component name and then we should just move the logic the use effect hook into those compon we do want to keep Circles and overlay functions still as separate modules. This is just a reorganization at the Jsx level to prepare for the idea of just taking that overlay component and using it elsewhere
