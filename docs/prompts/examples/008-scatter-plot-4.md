The problem here is that we are using D3 for render labels, which I don't think we really need to. Because if you look at renderLabels, it's using D3 to add text. And I'm glad we did this. I wanted to show you kind of how you would do that. But because it's static and we don't really need anything fancy from D3, this might be cleaner to do straight in React. So let's actually get rid of the renderViz utility and move that logic back into the core React component into a useEffect that calls renderCircles and renderAxes. But the renderLabels should just be removed, and that all should be done directly in React. In ScatterPlot.tsx currently, we only use React and JSX to set up the top-level SVG element. And while we're doing this, let me also address another issue, which is the organization of the DOM elements within the SVG. See, if you look at it now, we have all the circles as direct children of the SVG, and then siblings of the circle elements are the axes elements and the text labels. Now, it's kind of okay, but I want to set us up for success in the future with more layers and more complexity. So I actually like using JSX within our SVG to introduce group elements that just serve to organize the DOM elements. So I want to have one group element for the marks. Maybe give it a class of marks, so if we're inspecting the DOM, we can easily figure out what it is. That should contain the circles, and then another G element should be the guides, including the axes, one for X, one for Y, and maybe another group element just to contain all the text labels. That way everything would be nicely organized.