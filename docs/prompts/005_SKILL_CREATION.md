Create a custom skill for example creation. Call it the Example Creation Skill, and what that should do is describe the details of how our current set of examples is set up, including the idea that you need to update the index file whenever you create a new example, also including the idea that the examples have a numbering, and when we create a new example, we want to add one to the last number. The skill should explain that the examples live under src/examples and are numbered. And it should also encapsulate some of our best practices or our preferred preferences for how to organize code with React and D3, namely the number 10 example of hovering is a good exemplar of how we want to organize components into group elements within our SVG. And in general, for rendering visual marks, we prefer to use D3 selections, but if there's nothing dynamic about it, for example text overlays, we can do the rendering with JSX. So please create that new skill.