Please make the following refinements:

1. In `src/App.tsx`, this part should use `useCallback`:

```
 const handleSelectExample = (exampleName: string) => {
    setSearchParams({ example: exampleName })
  }
```

2. The "Left Sidebar - Navigation" part should be able to be hidden, and this should be part of the URL too, e.g. `hideSidebar=true`. And it should be possible to toggle its visibility on and off with the "Esc" key.

3. Let's put each example in its own subdirectory, as follows:

 * `src/examples/PseudoScatterPlot.tsx` should move to `src/examples/001-pseudo-scatter-plot/PseudoScatterPlot.tsx`
 * `src/examples/PseudoBarChart.tsx` should move to `src/examples/002-pseudo-bar-chart/PseudoBarChart.tsx`
 * `src/examples/PseudoLineChart.tsx` should move to `src/examples/003-pseudo-line-chart/PseudoLineChart.tsx`

4. Let's change the URL scheme to track the index of the example, not the name, so that we can rename them later without breaking links. Current: `http://localhost:5173/constructing-visualizations/?example=Pseudo+Scatter+Plot`, Desired: `http://localhost:5173/constructing-visualizations/?example=1`


5. We're using the wrong package for react router. Current: `'react-router-dom'`. Desired: `"react-router"`. Example:

```
import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router";
import App from "./app";

const root = document.getElementById("root");

ReactDOM.createRoot(root).render(
  <BrowserRouter>
    <App />
  </BrowserRouter>,
);
```

please uninstall `react-router-dom`, and replace it with `react-router`, and use the imports from the example above.